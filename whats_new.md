**Put the effort slider back in the composer.**

- A couple builds ago it was removed from the composer bottom bar by mistake — it was only meant to leave the gear menu (it now lives in the **Default** panel). Restored: the draggable effort slider is back, next to the model name in the composer, for changing the *current* chat's effort on the fly.
- The **Default** panel keeps its own effort slider for new-chat defaults — so effort lives in both places now, exactly as intended.
