**Dropped the YOLO corner badge.**

- The red "auto-approving everything" tag in the bottom-right corner is gone. YOLO mode already shows as the active mode right in the composer (the ⚡ Modes button), so the corner badge was redundant — the composer mode tag is your per-chat indicator now.
